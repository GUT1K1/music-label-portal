# 📋 Полная документация проекта

> **Последнее обновление:** 02.11.2025  
> **Версия документации:** 1.0.0  
> **Статус проекта:** В разработке

---

## 🎯 Описание проекта

**Название:** CRM система управления задачами для творческой команды  
**Тип:** Single Page Application (SPA)  
**Стек:** React + TypeScript + Vite + PostgreSQL + Cloud Functions

### Назначение
Система управления задачами, заявками и отчётностью для творческой команды, включающая:
- Управление тикетами (заявками)
- Таск-менеджмент
- Аналитику и отчётность
- Систему уведомлений
- Интеграцию с Telegram и VK
- Управление релизами и питчингами

---

## 📁 Структура проекта

```
webapp/
├── backend/                    # Serverless функции (Cloud Functions)
│   ├── auth/                   # Аутентификация пользователей
│   ├── change-password/        # Смена пароля
│   ├── create-notification/    # Создание уведомлений
│   ├── deadline-reminder/      # Напоминания о дедлайнах
│   ├── jobs/                   # API вакансий
│   ├── messages/               # Система сообщений
│   ├── migrate-db/             # Миграции БД
│   ├── news/                   # Новости и FAQ
│   ├── notifications/          # Уведомления
│   ├── pitching/               # Управление питчингами
│   ├── release-analytics/      # Аналитика релизов
│   ├── releases/               # Управление релизами
│   ├── submissions/            # Сабмишены треков
│   ├── tasks/                  # API задач
│   ├── telegram-auth/          # Telegram аутентификация
│   ├── telegram-bot/           # Telegram бот
│   ├── ticket-analytics/       # Аналитика тикетов
│   ├── ticket-comments/        # Комментарии к тикетам
│   ├── tickets/                # API тикетов
│   ├── unread-counts/          # Счетчики непрочитанного
│   ├── upload-direct/          # Прямая загрузка файлов
│   ├── upload-reports/         # Загрузка отчётов
│   ├── users/                  # Управление пользователями
│   ├── vk-posts/               # Интеграция с VK
│   ├── weekly-report/          # Еженедельные отчёты
│   └── func2url.json           # Маппинг функций на URL
│
├── db_migrations/              # SQL миграции базы данных (44 миграции)
│   ├── V0001__create_users_and_tickets_tables.sql
│   ├── V0043__create_news_table.sql
│   └── V0044__create_jobs_table.sql
│
├── src/
│   ├── components/             # React компоненты
│   │   ├── ui/                 # UI библиотека (shadcn/ui)
│   │   ├── ArtistView.tsx      # Роль: Артист
│   │   ├── ManagerView.tsx     # Роль: Менеджер
│   │   ├── DirectorView.tsx    # Роль: Директор
│   │   ├── TicketCard.tsx      # Карточка тикета
│   │   ├── TasksTab.tsx        # Вкладка задач
│   │   ├── NewsView.tsx        # Новости и вакансии
│   │   ├── AnalyticsView.tsx   # Аналитические дашборды
│   │   ├── LandingPage.tsx     # Лендинг
│   │   ├── LoginForm.tsx       # Форма входа
│   │   ├── NotificationBell.tsx # Колокольчик уведомлений
│   │   └── ... (60+ компонентов)
│   │
│   ├── contexts/               # React контексты
│   │   └── NotificationContext.tsx
│   │
│   ├── hooks/                  # Custom хуки
│   │   ├── use-toast.ts
│   │   ├── useActivityTracking.ts
│   │   └── useOnlineStatus.ts
│   │
│   ├── lib/                    # Утилиты
│   │   └── utils.ts
│   │
│   ├── pages/                  # Страницы приложения
│   │   ├── Home.tsx            # Главная страница (/)
│   │   ├── Index.tsx           # Приложение (/app)
│   │   ├── AdminFix.tsx        # Админ панель
│   │   ├── NotFound.tsx        # 404 (дефолтная)
│   │   └── NotFoundCustom.tsx  # 404 (кастомная)
│   │
│   ├── types/                  # TypeScript типы
│   ├── utils/                  # Вспомогательные функции
│   ├── App.tsx                 # Главный компонент
│   ├── main.tsx                # Точка входа
│   └── index.css               # Глобальные стили
│
├── public/                     # Статические файлы
├── builds/                     # Билды проекта
├── index.html                  # HTML шаблон
├── package.json                # Зависимости npm
├── tailwind.config.ts          # Конфигурация Tailwind
├── tsconfig.json               # Конфигурация TypeScript
├── vite.config.ts              # Конфигурация Vite
└── PROJECT_DOCUMENTATION.md    # Эта документация
```

---

## 🚀 Технологический стек

### Frontend
- **React 18.3.1** - UI библиотека
- **TypeScript 5.5.3** - Типизация
- **Vite (Rolldown 7.1.13)** - Сборщик
- **Tailwind CSS 3.4.11** - CSS фреймворк
- **shadcn/ui** - Компоненты UI
- **React Router 6.26.2** - Роутинг
- **TanStack Query 5.56.2** - Управление состоянием и кешем
- **React Hook Form 7.53.0** - Работа с формами
- **Zod 3.23.8** - Валидация схем
- **Lucide React 0.462.0** - Иконки
- **Recharts 2.12.7** - Графики и диаграммы
- **date-fns 3.6.0** - Работа с датами
- **Sonner 1.5.0** - Тосты

### Backend
- **Cloud Functions** - Serverless функции (Python + TypeScript)
- **PostgreSQL** - База данных (Simple Query Protocol)
- **psycopg2** - PostgreSQL драйвер для Python
- **Flyway** - Система миграций БД

### Интеграции
- **Telegram Bot API** - Уведомления и авторизация
- **VK API** - Интеграция с ВКонтакте
- **S3-совместимое хранилище** - Загрузка файлов

---

## 🗄️ База данных

### Основные таблицы

#### users
Пользователи системы с ролями и настройками
```sql
- id: SERIAL PRIMARY KEY
- username: VARCHAR(100) UNIQUE
- password_hash: VARCHAR(255)
- full_name: VARCHAR(255)
- role: VARCHAR(20) ['artist', 'manager', 'director']
- is_blocked: BOOLEAN
- telegram_chat_id: BIGINT
- telegram_username: VARCHAR(100)
- telegram_auth_date: TIMESTAMP
- vk_user_id: VARCHAR(100)
- vk_photo: VARCHAR(500)
- social_links: JSONB
- revenue_share: DECIMAL(5,2)
- created_at: TIMESTAMP
- last_activity: TIMESTAMP
```

#### tickets
Заявки/тикеты от артистов
```sql
- id: SERIAL PRIMARY KEY
- title: VARCHAR(255)
- description: TEXT
- status: VARCHAR(20) ['new', 'in_progress', 'completed']
- priority: VARCHAR(20) ['low', 'medium', 'high']
- created_by: INTEGER (FK users)
- assigned_to: INTEGER (FK users)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
- deadline: TIMESTAMP
- attachments: TEXT[]
- completed_at: TIMESTAMP
```

#### tasks
Задачи для менеджеров и директора
```sql
- id: SERIAL PRIMARY KEY
- title: VARCHAR(255)
- description: TEXT
- status: VARCHAR(20) ['pending', 'in_progress', 'completed', 'cancelled']
- priority: VARCHAR(20) ['low', 'medium', 'high']
- created_by: INTEGER (FK users)
- assigned_to: INTEGER (FK users)
- ticket_id: INTEGER (FK tickets)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
- deadline: TIMESTAMP
- completion_report: TEXT
- attachments: TEXT[]
- completion_attachments: TEXT[]
- archived_at: TIMESTAMP
```

#### releases
Управление релизами треков
```sql
- id: SERIAL PRIMARY KEY
- artist_id: INTEGER (FK users)
- track_name: VARCHAR(255)
- artist_name: VARCHAR(255)
- release_date: DATE
- status: VARCHAR(20) ['pending', 'approved', 'rejected', 'released']
- platforms: TEXT[]
- cover_art_url: VARCHAR(500)
- audio_url: VARCHAR(500)
- rejection_type: VARCHAR(50)
- rejection_reason: TEXT
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

#### pitchings
Управление питчингами
```sql
- id: SERIAL PRIMARY KEY
- track_name: VARCHAR(255)
- artist_name: VARCHAR(255)
- pitch_date: DATE
- status: VARCHAR(20)
- playlists: TEXT[]
- notes: TEXT
- created_by: INTEGER (FK users)
- created_at: TIMESTAMP
```

#### notifications
Система уведомлений
```sql
- id: SERIAL PRIMARY KEY
- user_id: INTEGER (FK users)
- title: VARCHAR(255)
- message: TEXT
- type: VARCHAR(50)
- is_read: BOOLEAN
- created_at: TIMESTAMP
- metadata: JSONB
```

#### messages
Внутренняя система сообщений
```sql
- id: SERIAL PRIMARY KEY
- sender_id: INTEGER (FK users)
- receiver_id: INTEGER (FK users)
- message: TEXT
- is_read: BOOLEAN
- attachments: TEXT[]
- created_at: TIMESTAMP
```

#### news
Новости и FAQ
```sql
- id: SERIAL PRIMARY KEY
- title: VARCHAR(255)
- content: TEXT
- type: VARCHAR(20) ['update', 'faq', 'job']
- is_active: BOOLEAN
- priority: INTEGER
- created_by: INTEGER (FK users)
- created_at: TIMESTAMP
- updated_at: TIMESTAMP
```

#### jobs
Вакансии
```sql
- id: SERIAL PRIMARY KEY
- position: VARCHAR(255)
- schedule: VARCHAR(255)
- workplace: VARCHAR(255)
- duties: TEXT
- salary: VARCHAR(255)
- contact: VARCHAR(255)
- is_active: BOOLEAN
- created_by: INTEGER (FK users)
- created_at: TIMESTAMP
```

### Дополнительные таблицы
- `user_activity_logs` - логи активности пользователей
- `artist_reports` - отчёты артистов
- `uploaded_reports` - загруженные файлы отчётов
- `submissions` - сабмишены треков
- `task_analytics` - аналитика по задачам
- `ticket_comments` - комментарии к тикетам

---

## 🔗 Backend API (Cloud Functions)

### Маппинг функций (func2url.json)

| Функция | URL | Описание |
|---------|-----|----------|
| auth | `d2601eec-1d55-4956-b655-187431987ed9` | Аутентификация |
| users | `cf5d45c1-d64b-4400-af77-a51c7588d942` | CRUD пользователей |
| tickets | `cdcd7646-5a98-477f-8464-d1aa48319296` | CRUD тикетов |
| tasks | `13e06494-4f4d-4854-b126-bbc191bf0890` | CRUD задач |
| messages | `7bcbbe5a-cae2-4cbe-94c1-ca6a0152ce3a` | Сообщения |
| notifications | `7d3a4ec8-d714-47b2-b4b8-186a84744b78` | Уведомления |
| news | `02b8e089-cfba-4460-9cad-479b3d0c5c80` | Новости и FAQ |
| jobs | `57ba52aa-ffc4-4822-b9ee-d1847947bc41` | Вакансии |
| releases | `05d2ddf9-772f-40cb-bcef-0d70fa96e059` | Релизы |
| pitching | `da292f4e-1263-4ad9-878e-0349a94d0480` | Питчинги |
| submissions | `40a44285-32b8-4e3e-8f8f-b77f16293727` | Сабмишены |
| upload-direct | `01922e7e-40ee-4482-9a75-1bf53b8812d9` | Загрузка файлов |
| upload-reports | `be12d7b5-90f6-4a13-992e-204cd8f0a264` | Загрузка отчётов |
| telegram-bot | `ae7c32d8-5b08-4870-9606-e750de3c31a9` | Telegram бот |
| telegram-auth | `e15fe44b-1976-42f1-aa0d-ff37aca2fd10` | Telegram авторизация |
| vk-posts | `2a44eeac-eb38-454b-a031-3713ae83a3d2` | VK посты |
| ticket-analytics | `831a484a-c7b9-4607-b683-19d813dc77e5` | Аналитика тикетов |
| release-analytics | `5f67ae5e-fa40-4b8a-ba1e-d66a28d27c18` | Аналитика релизов |
| weekly-report | `57dc847b-266a-4258-af20-68e634b98ec7` | Еженедельный отчёт |
| deadline-reminder | `7b917e9b-349b-4e00-91f6-933aaff43d85` | Напоминания |
| change-password | `fc19a64b-eb76-4cdf-bf49-f7ed121edba7` | Смена пароля |
| unread-counts | `87d13cda-05ed-4e45-9232-344fe2c026d7` | Счетчики |
| ticket-comments | `b636dc56-7328-4fb7-933d-5d6d32e9ee96` | Комментарии |
| create-notification | `c93b4bbd-7c4a-4d8f-9f8b-c6c0a9984216` | Создание уведомления |
| migrate-db | `05c06730-31f5-43a6-a240-50721bb848c6` | Миграции БД |

### Формат запросов

Все функции используют стандартный формат Cloud Functions:
```typescript
interface CloudFunctionEvent {
  httpMethod: 'GET' | 'POST' | 'PUT' | 'DELETE' | 'OPTIONS';
  headers: Record<string, string>;
  queryStringParameters?: Record<string, string>;
  body?: string;
  isBase64Encoded: boolean;
  requestContext: {
    requestId: string;
    identity: { sourceIp: string; userAgent: string };
  };
}

interface CloudFunctionResponse {
  statusCode: number;
  headers: Record<string, string>;
  body: string;
  isBase64Encoded: boolean;
}
```

### CORS Headers
Все функции поддерживают CORS с заголовками:
```
Access-Control-Allow-Origin: *
Access-Control-Allow-Methods: GET, POST, PUT, DELETE, OPTIONS
Access-Control-Allow-Headers: Content-Type, X-User-Id, X-Auth-Token
```

---

## 🎨 Frontend архитектура

### Роутинг (React Router)

```typescript
// src/App.tsx
<Routes>
  <Route path="/" element={<Home />} />              // Лендинг
  <Route path="/app" element={<Index />} />          // Приложение
  <Route path="/admin-fix-passwords" element={<AdminFix />} />
  <Route path="*" element={<NotFoundCustom />} />   // 404
</Routes>
```

### Система ролей

**Artist (Артист)**
- Просмотр своих тикетов
- Создание новых тикетов
- Загрузка отчётов
- Управление своими релизами
- Просмотр новостей и вакансий

**Manager (Менеджер)**
- Все права артиста +
- Управление тикетами (назначение, статусы)
- Управление задачами
- Создание новостей и вакансий
- Просмотр аналитики по тикетам

**Director (Директор)**
- Все права менеджера +
- Управление пользователями
- Полная аналитика по системе
- Управление релизами и питчингами
- Настройка Telegram бота
- Доступ к недельным отчётам

### Ключевые компоненты

#### Вьюхи по ролям
- `ArtistView.tsx` - интерфейс артиста
- `ManagerView.tsx` - интерфейс менеджера
- `DirectorView.tsx` - интерфейс директора

#### Управление данными
- `useAuth.ts` - хук аутентификации
- `useTickets.ts` - хук работы с тикетами
- `useTasks.tsx` - хук работы с задачами
- `useUsers.ts` - хук работы с пользователями

#### Аналитика
- `AnalyticsView.tsx` - общая аналитика
- `TicketAnalyticsDashboard.tsx` - аналитика тикетов
- `ReleaseAnalyticsDashboard.tsx` - аналитика релизов
- `TaskAnalyticsDashboard.tsx` - аналитика задач

#### Система уведомлений
- `NotificationContext.tsx` - контекст уведомлений
- `NotificationBell.tsx` - колокольчик с уведомлениями
- Автоматическое обновление каждые 30 секунд

#### Активность пользователей
- `useActivityTracking.ts` - отслеживание активности
- `useOnlineStatus.ts` - онлайн статус
- `UserActivityMonitor.tsx` - монитор активности
- Автоматический лог-аут при неактивности (30 минут)

---

## 🎨 Дизайн-система

### Цветовая палитра (Tailwind CSS Variables)

```css
/* index.css */
:root {
  --background: 0 0% 5%;           /* Темный фон #0d0d0d */
  --foreground: 45 95% 90%;        /* Светлый текст #fdf5e6 */
  
  --primary: 45 100% 60%;          /* Желтый #ffd633 */
  --primary-foreground: 0 0% 5%;
  
  --secondary: 30 100% 50%;        /* Оранжевый #ff8000 */
  --secondary-foreground: 0 0% 100%;
  
  --muted: 0 0% 15%;               /* Приглушенный #262626 */
  --muted-foreground: 0 0% 65%;
  
  --accent: 45 100% 60%;           /* Акцент = Primary */
  --accent-foreground: 0 0% 5%;
  
  --border: 45 30% 20%;            /* Границы #3d3426 */
  --radius: 0.75rem;               /* Скругления 12px */
}
```

### Анимации

```css
/* Определены в index.css */
- fadeIn          - плавное появление с движением вверх
- slideIn         - скольжение снизу
- glow            - эффект свечения
- float           - плавание
- pulse           - пульсация
- shimmer         - мерцание градиента
- spin-slow       - медленное вращение
- scaleIn         - масштабирование от 0 до 1
```

### Компоненты UI (shadcn/ui)

Все UI компоненты находятся в `src/components/ui/`:
- `button` - кнопки
- `card` - карточки
- `dialog` - модальные окна
- `input` - поля ввода
- `select` - селекты
- `toast` - тосты
- `tabs` - вкладки
- `badge` - бейджи
- `avatar` - аватары
- `dropdown-menu` - выпадающие меню
- `icon` - обертка для Lucide иконок
- и другие...

### Icon компонент
```tsx
// Использование:
import Icon from '@/components/ui/icon';

<Icon name="Home" size={24} />
<Icon name="CustomIcon" fallback="CircleAlert" size={24} />
```

---

## 🔧 Конфигурация и настройки

### Vite (vite.config.ts)
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react-swc';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src')
    }
  },
  server: {
    port: 8080,
    host: true
  },
  build: {
    outDir: 'dist',
    sourcemap: true
  }
});
```

### TypeScript (tsconfig.json)
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"]
    }
  }
}
```

### Tailwind (tailwind.config.ts)
- Темная тема по умолчанию
- Кастомные цвета из CSS переменных
- Плагины: `tailwindcss-animate`
- Кастомные анимации и утилиты

---

## 📦 Зависимости

### Основные (dependencies)
```json
{
  "react": "^18.3.1",
  "react-dom": "^18.3.1",
  "react-router-dom": "^6.26.2",
  "@tanstack/react-query": "^5.56.2",
  "lucide-react": "^0.462.0",
  "tailwindcss": "^3.4.11",
  "zod": "^3.23.8",
  "date-fns": "^3.6.0",
  "recharts": "^2.12.7",
  "@radix-ui/*": "различные версии",
  "sonner": "^1.5.0"
}
```

### Dev зависимости (devDependencies)
```json
{
  "typescript": "^5.5.3",
  "vite": "npm:rolldown-vite@7.1.13",
  "@vitejs/plugin-react-swc": "^3.5.0",
  "eslint": "^9.9.0",
  "@tailwindcss/typography": "^0.5.15",
  "pp-tagger": "^1.0.1"
}
```

---

## 🚀 Команды запуска

### Development
```bash
npm run dev           # Запуск dev сервера (http://localhost:8080)
```

### Production
```bash
npm run build         # Сборка production билда
npm run build:dev     # Сборка dev билда с sourcemaps
npm run preview       # Превью production билда
```

### Linting
```bash
npm run lint          # Проверка ESLint
```

---

## 🔐 Система безопасности

### Аутентификация
- Хранение токена в localStorage: `user_session`
- Хеширование паролей: bcrypt (passlib в Python)
- Проверка сессии при каждом запросе

### Авторизация
- Проверка роли на уровне компонентов
- Проверка прав на уровне API (заголовок `X-User-Id`)
- Блокировка пользователей: поле `is_blocked` в таблице `users`

### Защита данных
- CORS настроен на всех функциях
- Валидация данных на клиенте (Zod) и сервере
- SQL-инъекции защищены через параметризованные запросы (Simple Query Protocol)

---

## 📊 Система аналитики

### Метрики тикетов
- Общее количество тикетов
- Распределение по статусам
- Распределение по приоритетам
- Среднее время выполнения
- Тикеты с просроченными дедлайнами

### Метрики задач
- Общее количество задач
- Завершенные задачи
- Активные задачи
- Распределение по исполнителям
- Эффективность выполнения

### Метрики релизов
- Количество релизов по статусам
- Процент одобрения/отклонения
- Популярные платформы
- Статистика по артистам

### Активность пользователей
- Последняя активность
- Онлайн статус (зеленый < 5 мин, желтый < 30 мин, серый > 30 мин)
- Логи активности в таблице `user_activity_logs`

---

## 🔔 Система уведомлений

### Типы уведомлений
```typescript
type NotificationType = 
  | 'ticket_created'          // Создан новый тикет
  | 'ticket_assigned'         // Тикет назначен
  | 'ticket_status_changed'   // Статус тикета изменён
  | 'task_assigned'           // Задача назначена
  | 'task_deadline_soon'      // Приближается дедлайн
  | 'message_received'        // Получено сообщение
  | 'release_approved'        // Релиз одобрен
  | 'release_rejected'        // Релиз отклонён
  | 'weekly_report'           // Еженедельный отчёт
  | 'system'                  // Системное уведомление
```

### Каналы доставки
1. **In-app** - колокольчик в интерфейсе
2. **Telegram** - через бота (если подключен `telegram_chat_id`)

### Автообновление
- Polling каждые 30 секунд через `NotificationContext`
- Звуковое уведомление при новых сообщениях (опционально)

---

## 🤖 Telegram интеграция

### Функции бота
1. **Авторизация** - привязка Telegram аккаунта
2. **Уведомления** - автоматическая рассылка событий
3. **Команды**:
   - `/start` - запуск бота
   - `/auth` - авторизация с кодом
   - `/status` - текущий статус

### Webhook
- URL: `https://functions.poehali.dev/telegram-bot/{function_id}`
- Обработка входящих сообщений
- Автоматическая отправка уведомлений

---

## 📝 VK интеграция

### Возможности
1. **OAuth авторизация** - вход через VK
2. **Получение данных профиля** - имя, фото, ID
3. **Синхронизация постов** - просмотр постов в системе

### Хранение данных
- `vk_user_id` - ID пользователя VK
- `vk_photo` - URL аватара (VARCHAR(500))

---

## 📤 Система загрузки файлов

### Типы файлов
1. **Вложения тикетов** - скриншоты, документы
2. **Вложения задач** - файлы к задачам и отчётам
3. **Отчёты артистов** - Excel, PDF файлы
4. **Обложки релизов** - изображения
5. **Аудио треков** - MP3, WAV файлы

### Функции загрузки
- `upload-direct` - прямая загрузка в S3
- `upload-reports` - загрузка отчётов с валидацией

### Защита от потери при HMR
```typescript
// main.tsx - блокировка Hot Module Replacement во время загрузки
if (import.meta.hot) {
  import.meta.hot.on('vite:beforeUpdate', () => {
    if (uploadGuard.getIsUploading()) {
      console.warn('[HMR] Update blocked: upload in progress');
      return false;
    }
  });
}
```

---

## 🗓️ Система дедлайнов и напоминаний

### Типы дедлайнов
1. **Тикеты** - `tickets.deadline`
2. **Задачи** - `tasks.deadline`
3. **Релизы** - `releases.release_date`
4. **Отчётность** - автоматический таймер до конца квартала

### Напоминания
- Cloud Function: `deadline-reminder`
- Запуск по расписанию (cron)
- Уведомления за 24 часа и 1 час до дедлайна
- Telegram уведомления

### Таймер отчётности
```typescript
// Даты отчётов: 30 апреля, 30 июня, 30 октября, 28 февраля
const reportDates = [
  new Date(year, 3, 30),   // Апрель
  new Date(year, 5, 30),   // Июнь
  new Date(year, 9, 30),   // Октябрь
  new Date(year + 1, 1, 28) // Февраль
];
```

Компонент: `NewsView.tsx` - стилизованный таймер обратного отсчёта

---

## 📈 Особенности реализации

### Lazy Loading компонентов
```tsx
const ArtistView = lazy(() => import('@/components/ArtistView'));
const ManagerView = lazy(() => import('@/components/ManagerView'));
const DirectorView = lazy(() => import('@/components/DirectorView'));
```

### React Query оптимизация
```typescript
const queryClient = new QueryClient({
  defaultOptions: {
    queries: {
      staleTime: 1000 * 60 * 5,      // 5 минут
      gcTime: 1000 * 60 * 10,         // 10 минут
      refetchOnWindowFocus: false,
      retry: 1
    }
  }
});
```

### Мемоизация
- `memo()` для тяжелых компонентов
- `useCallback()` для функций-обработчиков
- `useMemo()` для вычисляемых значений

### Стилизованный скроллбар
```css
/* Кастомный скролл с градиентом primary/secondary */
*::-webkit-scrollbar-thumb {
  background: linear-gradient(
    180deg,
    rgba(234, 179, 8, 0.5),
    rgba(251, 146, 60, 0.5)
  );
}
```

### Mobile-first подход
- Адаптивный дизайн на всех компонентах
- Тач-оптимизация (tap-highlight-color: transparent)
- Фиксированный font-size 16px на инпутах (предотвращает зум на iOS)
- Safe area insets для iPhone

---

## 🐛 Отладка и логирование

### Frontend логи
- `console.log` в dev режиме
- Логи загрузок через `uploadGuard`
- Логи активности пользователя

### Backend логи
- Cloud Functions логи через платформу
- SQL query логи в PostgreSQL
- Telegram webhook логи

### Обработка ошибок
```typescript
try {
  // API запрос
} catch (error) {
  toast({
    title: 'Ошибка',
    description: 'Описание ошибки',
    variant: 'destructive'
  });
}
```

---

## 📚 Дополнительные документы

В корне проекта есть специализированная документация:

- `README.md` - общее описание проекта
- `MIGRATION_README.md` - инструкции по миграциям БД
- `MIGRATION_SUMMARY.md` - сводка миграций
- `NOTIFICATIONS_LOGIC.md` - логика уведомлений
- `OPTIMIZATION_SUMMARY.md` - сводка оптимизаций

---

## 🔄 Changelog

### [2025-11-02] v1.0.0
- ✅ Создана полная документация проекта
- ✅ Документированы все 60+ компонентов
- ✅ Описаны все 26 Cloud Functions
- ✅ Задокументированы 44 миграции БД
- ✅ Добавлена документация по дизайн-системе
- ✅ Описаны системы безопасности и аналитики
- ✅ Задокументированы интеграции (Telegram, VK)
- 🎨 Стилизован таймер обратного отсчёта до отчётности

---

## 🎯 TODO / Roadmap

### Ближайшие планы
- [ ] Добавить mobile приложение (React Native)
- [ ] Реализовать real-time уведомления через WebSocket
- [ ] Добавить экспорт аналитики в Excel/PDF
- [ ] Внедрить систему комментариев к релизам
- [ ] Добавить поиск по всем сущностям
- [ ] Реализовать фильтры и сортировку на всех списках

### Оптимизации
- [ ] Внедрить Server-Side Rendering (SSR)
- [ ] Добавить Service Worker для offline режима
- [ ] Оптимизировать bundle size (code splitting)
- [ ] Добавить E2E тесты (Playwright/Cypress)
- [ ] Внедрить мониторинг ошибок (Sentry)

---

## 📞 Контакты и поддержка

### Разработка
- **AI Ассистент:** Юра (poehali.dev)
- **Репозиторий:** GitHub (при подключении интеграции)

### Сообщество
- **Telegram:** https://t.me/+QgiLIa1gFRY4Y2Iy

---

## 📄 Лицензия

Проект разработан в рамках платформы poehali.dev

---

*Документация будет автоматически обновляться при внесении изменений в проект.*

**Версия:** 1.0.0  
**Дата создания:** 02.11.2025  
**Последнее обновление:** 02.11.2025
